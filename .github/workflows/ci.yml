name: CI - sapliy-testing

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: sapliy-testing/package-lock.json

      - name: Install dependencies
        run: cd sapliy-testing && npm ci

      - name: Build
        run: cd sapliy-testing && npm run build

      - name: Type check
        run: cd sapliy-testing && npm run typecheck

      - name: Run Tests
        run: cd sapliy-testing && npm run test